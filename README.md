# Web & Terminal Fundamentals

Tài liệu học tương tác — nền tảng trước DevOps dành cho vibe coder.

## Stack
- HTML/CSS/JS thuần — không cần framework
- SVG diagrams inline

## Chạy local
Mở thẳng file `index.html` bằng browser — không cần `npm install`.

## Deploy
Static site — deploy trực tiếp lên Vercel, không cần build step.
